<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
android:layout_width="fill_parent"
android:layout_height="fill_parent"
android:padding="2dp" >

<de.hdodenhof.circleimageview.CircleImageView
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/imageView"
    android:layout_width="80dp"
    android:layout_height="80dp"

    app:civ_border_width="1dp"
    android:paddingBottom="5dp"
    android:paddingTop="5dp"
    app:civ_border_color="#FFFFFF"/>

<TextView
    android:id="@+id/textViewPatientNumber"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginTop="12dp"
    android:fontFamily="monospace"
    android:textStyle="bold"
    android:textColor="#000000"
    android:layout_toRightOf="@+id/imageView"
    android:paddingBottom="10dp"
    android:text="txt"
    android:textSize="14dp" />

<TextView
    android:id="@+id/textViewNotes"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:fontFamily="monospace"
    android:textStyle="bold"
    android:maxLines="2"
    android:ellipsize="end"
    android:paddingRight="5dp"
    android:layout_alignLeft="@+id/textViewPatientNumber"
    android:layout_below="@+id/textViewPatientNumber"
    android:textColor="#000000"
    android:textSize="11dp" />

    <TextView
        android:id="@+id/Status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="monospace"
        android:layout_marginTop="14dp"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
     android:layout_toEndOf="@+id/textViewPatientNumber"
     android:gravity="center"
     android:textSize="10dp" />
<TextView
 android:id="@+id/Date"
 android:layout_width="wrap_content"
 android:layout_height="wrap_content"
 android:fontFamily="monospace"
 android:layout_marginTop="12dp"
 android:textColor="#000000"
 android:textStyle="bold"
 android:layout_alignBaseline="@+id/textViewPatientNumber"
android:layout_alignBottom="@+id/textViewPatientNumber"
 android:layout_alignParentRight="true"
 android:textSize="11dp" />

</RelativeLayout>
